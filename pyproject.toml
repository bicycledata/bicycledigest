[project]
name = "bicycledigest"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "leidenalg>=0.11.0",
    "matplotlib>=3.10.7",
    "pandas>=2.3.3",
    "pyyaml>=6.0.3",
]

[tool.pyright]
venvPath = "."
venv = ".venv"
